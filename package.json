{
  "name": "hochzeitswebseite-m-s",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:rules": "firebase emulators:exec --only firestore,storage \"node --test tests/security-rules.test.mjs\"",
    "emulators:local": "firebase emulators:start --config firebase.local.json --only firestore,storage"
  },
  "dependencies": {
    "@noble/hashes": "^2.2.0",
    "firebase": "^12.17.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^6.30.1"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.0.0"
  }
}
